#
# If you want to build my kernel by itself, copy and paste the lines below into 
# .repo/local_manifests/roomservice.xml at the bottom of the dependency lines 
# that should already be there.
#

....
....
....
....

  <!--Add these lines if you want to build PoKernel by itself (using 'make bootzip' command)-->
  <remove-project name="android_build" />
  <project path="build" name="android_build" remote="ls" revision="jflte"/>
  
</manifest>
